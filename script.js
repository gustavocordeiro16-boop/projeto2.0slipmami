const botoes = document.queryselectorall (button);

botoes.foreach (function (botao)}
   let curtiu = false;
   botao.addEventlistener (click, botaoclicado);
   function botaoclicado()}
   console.log (ful clicado);
   let texto = botao.queryselector(span);
   if( curtiu=== false)
     texto.textcontent++;
     curtiu=true;
     else 
     text.textContention
     curtiu = false ;